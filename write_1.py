with open("/tmp/test_thiru.txt","w") as f:
    f.write("written from Jenkins via GIT")
    f.write("written from Jenkins via GIT222")
    f.write("written from Jenkins via GIT333")
    f.write("written from Jenkins via GIT444")
    f.write("written from Jenkins via GIT555")
